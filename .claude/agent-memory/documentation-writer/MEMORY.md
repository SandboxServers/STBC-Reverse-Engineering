# Documentation Writer — Memory

Cross-session notes on STBC doc style, the doc-index map, and patterns that work for this codebase.

## Index

Entries appear here as the campaign progresses. Each entry links to a topic file in this directory; this index stays under 200 lines.

- [v5 named-function convention](v5-named-function-convention.md) — how to demote pre-v5 annotation-script names in catalog docs without dropping the addresses
- [v5 foundation claim patterns](v5-foundation-claim-patterns.md) — evidence-row patterns for totals, address ranges, and exhaustive partitions in foundation-tier docs
- [catalog row disposition tree](catalog-row-disposition-tree.md) — four-bucket decision tree (keep / keep-as-is / move to internal-only / drop) for pre-v5 catalog rows during re-validation
- [verified status criteria](verified-status-criteria.md) — when a foundation doc qualifies for `verified` vs `partial`; pattern extrapolation as a valid medium-confidence justification
- [vtable doc render patterns](vtable-doc-render-patterns.md) — 4 patterns for vtable reference docs: address reassignment, two-stage construction, GetRTTI cross-check, vtable-size vs object-size disambiguation
- [TG vtable render patterns](tg-vtable-render-patterns.md) — 5 TG-specific patterns: __purecall reclassification, universal slot inheritance, type-ID constants table, sibling cross-link section, zero-xref negative claim format
- [process-meta doc pattern](process-meta-doc-pattern.md) — process-meta docs (coverage reports, pass narratives) validate by content removal, not address re-anchoring; three-class section taxonomy + status-partial-not-verified rule
- [cross-source doc render patterns](cross-source-doc-render-patterns.md) — two-tag convention ([v5-validated] for stbc.exe vs [cross-source-YYYY-MM-DD] for external corpora); intro NOTE block; address:null + file:line in note for external-corpus evidence rows
- [leaf doc render patterns](leaf-doc-render-patterns.md) — 8 patterns for leaf docs (event system, UI hierarchy): dropped-name NOTE block, anchored-vs-inferred methodology, universal-slot disclosure, indirection diagrams, dual sub-struct tables, Two-RTTI-Systems disclosure, partial-not-verified rule for corrections, Open-Questions debt list
- [load-bearing correction disambiguation](load-bearing-correction-disambiguation.md) — pattern for "two distinct globals were conflated" corrections: dedicated disambiguation subsection near doc top, two-row table, flag CLAUDE.md batch correction in body, cross-doc impacts in tracker
- [address-first authoring](address-first-authoring.md) — verified low-drift pattern: lead each per-function entry with hex address, prose follows; clean-validation precedent set by decompiled-functions.md (0 corrections / 0 drops out of ~50 entries)
- [engine family close batch](engine-family-close-batch.md) — four batched tasks pending after engine doc #10 lands: CLAUDE.md Key Globals two-row correction, CLAUDE.md naming-coverage refresh, README index check, netimmerse-vtables.md NiAVObject 0xC4→0xC8 reconciliation
- [protocol family inventory](protocol-family-inventory.md) — 22-doc protocol-family inventory pattern: 3 foundations + 10 mid + 9 leaves; 8 anchor sub-tables; top 5 cross-doc disagreements (FUN_005a2030 conflict, ship+0x2BC slot, TGEvent vtable slot count, CF16 doc overlap, duplicate hash tables); cross-source claim density higher than engine
- [protocol hub doc render](protocol-hub-doc-render.md) — 5 patterns for hub/index doc rendering: bit-pack-correction NOTE (Pattern 1), DATA-only xref disclosure for handler-registration tables (Pattern 2), retired-subsection cross-link (Pattern 3), dispatcher-row completeness annotation (Pattern 4), layout-table correction with IMPORTANT block (Pattern 5)
- [foundation doc class-identity inversion](foundation-doc-class-identity-inversion.md) — 6 patterns for rendering a foundation doc when v5 validation flips a class-identity claim that cascades into already-validated companions: NOTE-block headline naming inversion, dedicated "Two X Classes" preamble, address-keep/name-flip rule, handler-pattern transcript with role labels, dedicated "Cross-doc Reconciliation Required" table near bottom, wire-fact-vs-open-question separation
- [transport-foundation render patterns](transport-foundation-render-patterns.md) — 5 patterns for transport/framing/reliability layer docs: newly-located-algorithm dedicated subsection, function-creation NOTE block (when Ghidra had to `create_function`), side-channel-path sibling subsection (loop-back / queue-bypass), MTU-promote-to-intro, three-site agreement table for cross-coordinated state flags (SET / READ / WIRE)
- [opcode catalog render patterns](opcode-catalog-render-patterns.md) — 5 patterns for opcode-catalog reference docs (one per dispatcher / jump table) when the binary is clean: effective-event-code column-header asymmetry rename + taxonomy table, shared-thunk and shared-handler-distinct-arg annotations, dispatcher-default consolidation row, open-debt inline annotation for opcodes without leaf docs, trace-counts inline-tagged (not given their own column)
- [dispatcher subordinate render patterns](dispatcher-subordinate-render-patterns.md) — 5 patterns for small dispatcher-subordinate docs (NetFile-style sub-catalogs) when validation surfaces 2 material corrections: two-correction NOTE-block headline with C1/C2 tags, missing-handler negative-claim subtable, wire-observed-but-binary-unlocated dedicated subsection, IMPORTANT block on role-swap sections, dedicated Open-Questions section with OQ# inline tags + promotion-path disclosure
- [python-messages render patterns](python-messages-render-patterns.md) — 6 patterns for "cleanest pre-v5" mid-tier doc render (1 material correction + 2 name cascades + 3 cross-source tags + python-source tags): three-tag NOTE triage, SWIG-wrapper consolidation table, width-correction-not-stock-invalidation IMPORTANT block, name-cascade-no-body-rework rule, table-header python-source tagging, annotations-summary near-end section
- [architectural reframe render patterns](architectural-reframe-render-patterns.md) — 6 patterns for mid-tier docs where v5 flips an architectural framing (count-of-mechanisms / ownership re-attribution): C-tagged NOTE block, dedicated count-flip section EARLY, replacement-section-rename-with-old-name marker, per-handler table with trace-ratio column, OQ "call-site located vs semantic unknown" disambiguation, three-tag provenance; learned from tgmessage-routing.md "two becomes three relay mechanisms" reframe
- [stateupdate render patterns](stateupdate-render-patterns.md) — 9 patterns for "zero material corrections" v5 mid-tier docs: bolded zero-corrections headline in NOTE, function-creation-in-Ghidra disclosure (address right, Ghidra hadn't found the entry), wire-format vs validation-gate disambiguation rule, NOT-numeric-order callout for dirty-flag emit sequences, speculation-dropped explicit note, two-state-buffer disclosure for receivers, cross-source-tagged inline trace counts, Open-Questions section structure, low-effective-score disclosure with reason; learned from stateupdate.md (protocol mid #8, ~120 claims clean)
- [thin handler-index render patterns](thin-handler-index-render-patterns.md) — 7 patterns for thin handler-index docs (smallest mid-tier, delegates per-class detail to sibling) when v5 surfaces direction/symmetry refinements: R1/R2-tagged NOTE block, Sender-vs-Receiver Symmetry 3-row table near doc top, paired SENDER+RECEIVER pseudocode blocks, Authority 2-row sender table discharging "no C->S sender" negative claim, Cross-anchor references table near bottom, sibling-doc delegation note: in dispatch evidence row, address: null for relay/topology negative claims; learned from object-replication.md (protocol mid #9, 6/6 clean + 2 wording refinements)
- [objcreate-serialization render patterns](objcreate-serialization-render-patterns.md) — 8 patterns for mid-tier wire-format docs with 3 corrections + 4 refinements + Two-Pass architectural finding: same-total-bytes-order-inverted callout for wire-order C-fix, anchor-swap-not-new-fact framing for struct-field C-fix, Mermaid Two-Pass diagram with highlighted data-dependency nodes, closed-OQ disposition (drop from OQ + inline tag), cross-doc disagreement resolution with re-check flag, global-conflation R1 as inline caveat block, gate-refinement R2 with practical-impact note, cross-companion ahead-of-pass refinement flag (batched at family close); learned from objcreate-serialization.md (protocol mid #10)
- [named-slot table render patterns](named-slot-table-render-patterns.md) — 6 patterns for docs where v5 finds a partial-enumeration error in a named-slot table + function misattribution C2: switch-decoded slot table with property-ID column, missing-row bold-add convention, source-of-truth annotation naming the Ghidra function + plate, "created this pass" Ghidra disclosure for `create_function`-ed bodies, vtable-confusion C2 pattern (correct + incorrect attribution in NOTE + evidence note), refinement as numbered section (R3 vtable-consumer-count); learned from stateupdate-subsystem-wire-format.md (protocol mid #11)
- [large-catalog sampling render patterns](large-catalog-sampling-render-patterns.md) — 7 patterns for LARGEST-protocol-doc-class catalog docs (~250 claims, 16 parallel entries) with ZERO material corrections via N=4 sampling: sampling-strategy NOTE headline (cycle byte totals in bold), dedicated Sampling Strategy subsection near top with 4-axes check + ship-coverage rationale + promotion-path, Summary Table validation-column convention, per-section header tag with Cross-source: line, refinement blockquotes inserted at the column they refine (not bottom), hand-computed cycle totals embedded under sampled tables only, Open-Questions promotion-path-question always last; foundation cross-anchor non-cascade disclosure pattern; learned from per-ship-subsystem-wire-format.md (protocol mid #12)
- [class-identity mid-tier render patterns](class-identity-mid-tier-render-patterns.md) — 7 patterns for mid-tier class-reference docs when v5 surfaces (a) fabricated-parent C1 + (b) vtable slot-count C2 with newly-discovered slots + (c) annotation-script drift C3 + (d) substantial Ghidra creation work: NOTE-block bold-headline triage, dedicated "Class Hierarchy (Corrected)" section with 4-claim falsification list, sibling-class differentiator table with IsA chain row, vtable section with 4-column bolding on new slots + cross-link for universal slot 8, universal-pattern callout subsection (SWIG triple-string), Ghidra Annotations Applied section with "newly created" subdivision and Reason column disclosure, Open Questions section with anchor+statement+suspect+requires format; tag set [v5-validated YYYY-MM-DD] + **NEW this pass** + (medium confidence); learned from tgobjptrevent-class.md (protocol mid #13, last mid-tier of protocol family)
- [leaf cascade render patterns](leaf-cascade-render-patterns.md) — 9 patterns for FIRST-leaf protocol docs (5 corrections, 0 wire-byte changes) when v5 cascade from mid-tier flips a class-identity claim that propagates to multiple sibling classes: two-not-three producer reframe (P1), sibling cascade in hierarchy correction (P2), IsA chain "Confirmed at" column (P3), source-vs-dest encoding asymmetry 2-row table (P4), in-memory vs wire-size disambiguation block (P5), receiver-flow function-rename inline note (P6), Stream-vtable slot-map appendix mapping slots to extensions (P7), producer-undefined-in-DB completeness:null disclosure (P8), vtable slot-count conflict NOTE with confidence:low row downgrades (P9); learned from pythonevent-wire-format.md (protocol leaf #14, first leaf of protocol family)
- [first-verified-leaf render patterns](first-verified-leaf-render-patterns.md) — 7 patterns for FIRST PROTOCOL DOC TO REACH `verified` status (1 byte-level typo + 1 wording refinement, 110+ claims clean): "first to reach verified" NOTE headline (P1), bolded-critical-OpenBC-finding callout inside NOTE (P2), dedicated `## Server-Side Authority Note` IMPORTANT subsection with negative-claim bullet evidence (P3), address-as-constant pattern NOTE block (P4), real-code-but-undefined-fn disclosure citing prologue bytes + naming prior-leaf precedent (P5), damage-constants in-line byte-verified table directly under the formula (P6), Ghidra Annotations Applied verified-tier shape (3 short subsections, no "newly created" subdivision, plates one-liner) (P7); plus what-NOT-to-do guards: no Open-Questions blocking section, no every-paragraph tags, no body restructure, tracker row needs inline corrections summary + §6.N cross-link; learned from collision-effect-protocol.md (protocol leaf #15, first protocol family doc to clear `verified`)
- [verified-leaf-with-cascade render patterns](verified-leaf-with-cascade-render-patterns.md) — 8 patterns for the SECOND protocol-family doc to reach `verified` when it combines 0 wire-byte changes + 3 minor non-wire corrections + 4 newly-created Ghidra functions (different shape than first-verified-leaf): multi-correction NOTE-block triage with (C1)/(C2)/(C3) tags (P1), cascade-correction in-body NOTE block citing originating docs (P2), Ghidra-symbol-vs-binary-string in-body NOTE for typography corrections (P3), "CREATED this pass" inline tag in Related Functions Role column + "(formerly documented as X)" for renames (P4), Ghidra Annotations split into 3 subsections with Reason column for 4+ created functions (P5), universal-pattern systematic disclosure naming prior leaves (P6), Open Questions allowed with promotion-path framing for low-priority items (P7), inline `[low-confidence — session-dependent]` flag for variable stats (P8); plus rule: don't promote minor corrections to body subsections (that's `partial` shape); learned from set-phaser-level-protocol.md (protocol leaf #16, second protocol doc to clear `verified`)
- [architectural discovery render patterns](architectural-discovery-render-patterns.md) — 9 patterns for leaf docs where v5 surfaces a MAJOR architectural finding (e.g., a second class registry) that closes a foundation-doc OQ + creates load-bearing shared anchor material: NOTE-block leads with discovery NOT corrections (P1), dedicated `## Two-Registry Architecture`-style section as second `##` with IMPORTANT block citing foundation OQ closure (P2), 4-column registry comparison table (P3), cluster sub-table for confirmed siblings with unknowns explicitly promoted to Open Questions (P4), per-byte sender-source table with [v5-validated YYYY-MM-DD] tag even when wire format is unchanged (P5), dedicated "Critical correction: FUN_X sends opcode Y NOT Z" subsection with disasm bytes for attribution-flip (P6), name-collision subsection with 2-row Address-Type-Role-When-runs table (P7), stock-trace decode re-tagged inline with bolded emphasis on semantic-changed fields (P8), Cross-doc impacts section naming family-close batch work without modifying companions (P9); plus rule: §4 cross-doc disagreements get "**CLOSED (YYYY-MM-DD, leaf #N):**" annotation with binary-truth one-liner, NOT deleted; learned from delete-player-ui-wire-format.md (protocol leaf #17, MAJOR finding closed wire-format-spec OQ #2)
- [command-message-triad render patterns](command-message-triad-render-patterns.md) — 8 patterns for leaf docs where v5 surfaces (a) command-message-vs-event-message architectural distinction, (b) physical-constant re-identification with gate-semantic inversion (DAT was FLT_MAX, sentinel not threshold), (c) literal-string re-identification with sender-suppression inversion ("warp" tunnel sentinel), AND closes a prior §4 conflict in the same pass: NOTE-block category-grouped not severity-ranked (P1), dedicated "Command Messages vs Event Messages" subsection IMMEDIATELY after Overview with bolded-own-opcodes per-opcode comparison table (P2), per-correction subsection placed AT THE CONTEXT not bottom appendix (P3), FLT_MAX gate inversion gets explicit ctor->mutate->gate mechanism trace + dedicated "OpenBC implication" paragraph (P4), string-constant re-identification REWRITES the section heading not appends (P5), address-mapping C4/C5 collected in shared `## Critical Correction: Function Address Map` near bottom + inline annotations in final table (P6), §4 closure tagged BOTH in body (parenthetical pointer to tracker) AND in tracker (back-link to leaf subsection) (P7), negative claim for "bypasses TGFactory" added as top-tier evidence row with address:null + searched-bodies note (P8); plus rule: don't downgrade FLT_MAX-or-threshold correction to LOW severity just because wire format is unchanged — the gate semantic inverts and that's HIGH OpenBC impact; learned from objnotfound-requestobj-enterset-wire-format.md (protocol leaf #18, closed §4 #1 FUN_005a2030 conflict)
- [foundation-cascade leaf render patterns](foundation-cascade-leaf-render-patterns.md) — 7 patterns for leaf docs where ALL v5 corrections cascade from a previously-validated foundation doc (no wire-byte changes, 0 algorithm changes) and the leaf's catalog table got its identity column rewritten: NOTE-block "ONE material correction" with "offsets right, labels wrong" disambiguation (P1), dedicated C1 section as 2nd `##` heading with archaeological-finding subsection explaining WHY offsets stayed valid e.g. container-aliasing via zero-fill range (P2), bolded-row catalog table with Prior Label column + authority citation immediately after (P3), invalidated-negative-claim called out in dedicated subsection with verbatim quote + numbered errors + replacement statement (P4), Clarifications as `### Clar-N` headings inline at their refinement context NOT centralized at bottom (P5), container-aliasing pattern deserves its own subsection for cross-doc reconciliation (P6), kick path action chain as dedicated `## Kick Path` heading with addresses + "CREATED this pass" inline disclosure (P7); plus rule: don't restructure body for cascade-only corrections — preserve original section order (readers may have inbound links); learned from subsystem-integrity-hash.md (protocol leaf #19, closed §4 #4 + §4 #5 simultaneously via foundation-pass + leaf-pass combination)
- [batched sibling render patterns](batched-sibling-render-patterns.md) — 8 patterns for rendering TWO sibling docs in a single pass when they share the same Ghidra anchors and the archaeology specialist's evidence packet is combined: single shared packet + two NOTE blocks with category-grouped headlines (P1), shared frontmatter rows + per-doc extras (P2), in-context full-render of shared structures with explicit cross-link headers (P3), two strategies in two docs with mutual cross-link (P4), internal-inconsistency correction trumps external-inconsistency e.g. 14-byte total inconsistent with field-size narrative (P5), enumerate the xref table when narrative says "N call sites total" (P6), §4 closure annotation "addressed via cross-links rather than merge" when pre-v5 suggested merge (P7), §6 entries paired but each names the OTHER as batch partner (P8); plus rule: don't downgrade wire-format byte-size error to LOW severity just because algorithm unchanged — HIGH severity for OpenBC if pre-v5 wire frame was wrong; learned from cf16-precision-analysis.md (#20) + cf16-explosion-encoding.md (#21) rendered as a batch on 2026-05-28 (third + fourth protocol-family docs to clear `verified`)
