# Game Archaeology Specialist — Memory

Field journal for binary archaeology on stbc.exe. The binary is finite; every dig builds the shared map.

## Index

- [Engine Snapshot 2026-05-28](engine-snapshot-20260528.md) — v5 campaign ground truth: binary fingerprint, naming coverage (25.7%, not 83%), annotation scripts never applied to current import, top 3 drift findings
- [Dispatcher Recovery 2026-05-28](dispatcher-recovery-20260528.md) — MpgameHandleMessage at 0x0069F2A0 recovered (Ghidra missed it: DATA-only xref); jump table decoded; v5 patterns for callback-registered functions, plate header format, __thiscall prototype gotchas
- [Struct Skeletons 2026-05-28](struct-skeletons-20260528.md) — MultiplayerGame (0x200), TGMessage (0x2C), TGBufferStream (0x2C), PlayerSlot (0x18) in Ghidra DB; dispatcher 69.94->79.0; HostMsgHandler +30 by typing; field-anchor patterns
- [TGBufferStream Vtable 2026-05-28](tgbufferstream-vtable-20260528.md) — vtable[0]=GetStreamTypeId returns 0x32 (95.0 score); sizeof grown 0x2C->0x40; 8 vtable slots named; cursor/pad open Q resolved; dispatcher 0x32 claim medium->high
- [NiRTTI Factory Validation 2026-05-28](nirtti-factory-validation-20260528.md) — 117 registrations EXACTLY (237 xrefs - 2 consumer reads - 1 standalone destructor / 2); 2 TG (TGDimmerController + TGFuzzyTriShape, NOT TGOverlayController); pattern 100% consistent across 14MB range; 17 DAT_-stub factories are abstract bases; guard flag column systematically wrong; NiAlloc=0x00718cb0 (NOT 0x00717840 as engine-snapshot says); NiNode vtable=0x00898f2c
- [NetImmerse Vtables Validation 2026-05-28](netimmerse-vtables-validation-20260528.md) — 6 vtable anchors confirmed; doc's 0x00899264 is NiTriBasedGeom (NOT NiTriShape); canonical NiTriShape vtable is 0x00899374 (missing from doc); ctor chain decompiled; __purecall=0x00859a0b confirmed; slot 11 universal no-op (0x0040da50=`ret`)
- [TG Hierarchy Vtables Validation 2026-05-28](tg-hierarchy-vtables-validation-20260528.md) — 9-vtable Ship inheritance chain end-to-end via 8 ctor decompiles; TGObject vtable 0x00896278 correction holds (0x008963BC has ZERO xrefs, NOT TGHashTable); universal slot-1 GetTypeID class-ID scheme catalogued; "NULL stub" doc text -> __purecall correction; Ship 92-slot boundary at 0x008944AC + adjacent floats
- [Gamebryo Cross-Reference Validation 2026-05-28](gamebryo-cross-reference-validation-20260528.md) — MWSE-equivalence claim DEBUNKED: NI 3.1 NiAVObject=0xC8 (not 0x90), NiNode=0xE8 (not 0xB0); +0x38 delta lives in V3.1-only Velocity + Has Bounding Volume + Bounding Volume fields. nif.xml version-conditional claims all hold (7 lines verified). NiKeyframeManager mis-categorized as "NI 3.1-only" (exists in Gb 1.2 as deprecated). 42/21 class-count arithmetic doesn't add up (subcategories sum to 45/20). First cross-source doc under v5.
- [Event System Validation 2026-05-28](event-system-validation-20260528.md) — Doc #8: TGCallback (0x008960f4, 0x14B) + TGConditionHandler (0x00896104, dual-array) + TGInstanceHandlerTable (0x00896030, lazy, 37 buckets, 0x94B array) + TGEvent (0x00896018, 0x28B) vtable+size anchored. TGEventManager singleton at global 0x00991438. CORRECTION: doc's "TGEvent factory ID 0x02" wrong — that's TGObject's tag; TGEvent uses string-pointer RTTI. Save/Load/Fixup method names unanchored.
- [UI Class Hierarchy Validation 2026-05-28](ui-class-hierarchy-validation-20260528.md) — Doc #9: TopWindow global is 0x009878cc (NOT 0x0097e238 — that's PlayWindow/Game). MultiplayerGame +0x74 playerSlots (struct-skeletons was WRONG at +0x70), +0x1F8 byte readyForNewPlayers, +0x1FC dword maxPlayers, 0x200 total. MainWindow type-ID catalog expanded 8 -> 12 (adds 3, 4, 6=MapWindow); doc's "PlayWindow type 5 ctor 0x00405c10" REFUTED — FUN_00405c10 is freestanding, type 5 ctor is FUN_00507900. STWidget + STRadioGroup have NO binary string (likely RE inventions). FUN_0050ca50 = TopWindow event-handler registration goldmine (16+ IDs with named handlers). TGUIObject +0x14 parent / +0x18 bounds / +0x28 flags (init 8=visible) / +0x2C callbacks all confirmed via FUN_0072dcc0 + parent FUN_0072fc20. 0x10000000 layout-guard bit confirmed at FUN_00732120.
- [Decompiled Functions Validation 2026-05-28](decompiled-functions-validation-20260528.md) — Doc #10 (FINAL engine doc): all 34 unique addresses exist as functions; 5 of 5 behavioral spot-checks PASS (InitMultiplayer, NetFile ctor, ReceiveMessageHandler, ChecksumCompleteHandler, TGNetwork::Update); cross-doc consistency 100% (MultiplayerGame +0x74/+0x1F8/+0x1FC match doc #9; EventManager+0x2C registry at 0x0097F864 confirmed byte-level via `MOV ECX,0x97f864` at 0x006a31bf); MainTick does NOT call TGNetwork::Update confirmed; zero corrections, zero drops — cleanest doc in campaign. Recommend `verified` status + rename to `multiplayer-decompiled-functions.md` (scope is MP/network/checksum/event, not general).
- [Protocol Snapshot 2026-05-28](protocol-snapshot-20260528.md) — Phase 1 reconnaissance for v5 docs/protocol/ campaign: all 3 dispatchers + 16 game-opcode handlers + 6 NetFile sub-handlers + 3 MP-window handlers + 16 stream-primitive helpers + 6 CF16/CV addresses + reliable pipeline (006b61e0/006b64d0) + SendStateUpdates (006b55b0) + AlbyRules anchor (006c2280) all confirmed present in Ghidra. NetFile + MP-window dispatchers decoded this session. Top drift: (1) stream-primitives.md conflates TGBufferStream with separate bit-packing wrapper at 006cefe0 (different field offsets); (2) CLAUDE.md TopWindow@0x0097e238 is actually PlayWindow; (3) 5 load-bearing protocol functions at effective <10. TGObjPtrEvent/TGCharEvent class vtables NOT located — only SWIG type-info tables + RTTI stubs confirmed.
- [Wire-Format-Spec Validation 2026-05-28](wire-format-spec-validation-20260528.md) — Protocol doc #1 (hub). All 41 game opcodes + 3 dispatchers + 6 NetFile + 29 event handlers confirmed (24 are LAB_ labels Ghidra missed; identity proved via FUN_0069efe0 registration-string decompile). Two CORRECTIONS: Settings packet uses WriteBit not WriteByte for the 2 settings bytes + checksumFlag (006cf770 is WriteBit); ship+0x2BC is Pulse Weapon System, ship+0x2D4 is Tractor (proven by FUN_005b5030 weapon-type 0x802A-0x802F switch). Anti-cheat hash CONFIRMED dead in MP (FUN_005b17f0 bVar17 = !isMultiplayer gate). Subsystem-integrity-hash.md is canonical for the hash table; wire-format-spec keeps summary+link. TopWindow drift does NOT apply (doc never cites 0x0097e238). Status: partial pending corrections.
- [Stream-Primitives Validation 2026-05-28](stream-primitives-validation-20260528.md) — Protocol doc #2 (foundation). Resolves two-class confusion in OPPOSITE direction from snapshot: the 0x30-byte class at 0x006CEFE0 / vtable 0x00895C58 IS the SWIG TGBufferStream (sizeof via `new_TGBufferStream` PUSH 0x30 at 0x005C22A0). Field +0x1C buf / +0x20 cap / +0x24 cursor / +0x28 bookmark / +0x2C bit-mask CONFIRMED. All 14 primitive addresses + bit-pack format `[count:3][bits:5]` (count stored as actual 1..5) + CF16 algorithm CONFIRMED. ONE correction: CV3 wire format is 3 bytes (direction-only), NOT 5 bytes — FUN_006D2EB0 only calls vtable[0x50] 3x. The 0x40-byte class at 0x006B82A0 (vtable 0x008958D0, tag 0x32) is NOT TGBufferStream — it's the wire-container (likely TGStreamedObject). REFUTES prior tgbufferstream-vtable-20260528 mis-labeling. 16 renames `TGBufferStream_swig_*` + plates on WriteBit/ReadBit/Ctor/CF16Encode/CF16Decode. Status: partial pending CV3 correction + class-B re-id.
- [Transport-Layer Validation 2026-05-28](transport-layer-validation-20260528.md) — Protocol doc #3 (foundation). AlbyRules cipher transform LOCATED: Encrypt 0x006C2490 (vtable[1]) + Decrypt 0x006C2520 (vtable[2]) + InitKey 0x006C2280 + Step 0x006C22F0; both Encrypt/Decrypt re-key on every packet (no streaming state — UDP loss/reorder cannot desync client/server). SendPacket 0x006B9870 + ReceivePacket 0x006B95F0 CREATED in Ghidra (were undefined; auto-analysis missed them — no plain CALL xrefs, dispatched via vtable[27]/[28] of TGWinsockNetwork). 7 transport types factory registration confirmed exactly. Type 0x32 + 0x01 wire formats byte-by-byte verified. Below32 ACK bit (TGHeaderMessage+0x40) confirmed at 3 sites: SET in HandleReliableReceived, READ in HandleACK, WIRE in Serialize. CORRECTIONS: seq counters at peer+0x26/+0x2A (not +0x98/+0xA8 — doc confused with network+0xA8=0x8000 threshold); NetFile dispatcher opcodes are 0x20,0x21,0x22,0x23,0x25,0x27 (not contiguous); doc's Appendix A "TGBufferStream Layout" is actually SWIG TGBufferStream (replace with cross-link). NEW: MTU=1024 (network+0xAC=0x400), SendPacket self-send loop-back (this+0x1C dest → local queue +0x33C/+0x340, ReceivePacket drains via toggle +0x344). 20 renames + 7 plate comments. Open Qs: FragmentMessage total_frags placement on head vs tail; connection state 1 unverified.
- [Game-Opcodes Validation 2026-05-28](game-opcodes-validation-20260528.md) — Protocol doc #4 (mid). FAST validation — dispatcher recovery did all the heavy lifting. 41-entry jump table re-decoded byte-by-byte; 9 handler bodies spot-checked. ZERO binary corrections. ONE clarification: doc's "Recv Event Code" column conflates dispatcher PUSH overrides (9 opcodes get a constant) with wire-payload event codes (0x0C/0x11/0x12 keep wire value because PUSH=0). FUN_0069FDA0 invariant: `if (param_2 != 0) puVar7[4] = param_2;`. CONFIRMED: 0x14 wire format (i32v); 0x15 re-posts as event 0x008000FC + distance gate _DAT_008955c8; 0x29 reads radius before damage (CF16 fStack_50 first, fStack_54 second, passed to FUN_004BBDE0); 0x06/0x0D share FUN_0069F880 (TGEvent factory chain); 0x16 routes via MultiplayerWindow (default cleanup here); 0x04/0x05 dead. Open Qs: 0x18 wire format (mirror OpenBC); event-manager singleton (0x991438 vs 0x97F838); session-frequency tags needed.
- [Checksum-Opcodes Validation 2026-05-28](checksum-opcodes-validation-20260528.md) — Protocol doc #5 (mid). TWO material corrections: C1 0x22/0x23 dialog swap (binary: 0x22="SystemChecksumFail" per-file, 0x23="VersionDifferent" App.pyc reference — doc had them inverted); C2 "5th round 0xFF Scripts/Multiplayer" fabrication (FUN_006A3820 emits only indices 0..3; Scripts/Multiplayer is bundled into round-0's reference hash via FUN_006A6630). Refinements: 0x25 dialog is first-shot only; 0x21 payload structure decoded (not opaque). NEW: 0x28 sender=FUN_006A5860; event IDs 0x8000E6 (Settings trigger), 0x8000E7 (fail), 0x8000E8 (success) each have exactly 2 xrefs (producer + FUN_0069E590 registration); confirms wire-format-spec.md C1 (WriteBool_Bit for Settings flags). Open Q: who sends round-0xFF (real in packet traces; binary sender unlocated).
- [Python-Messages Validation 2026-05-28](python-messages-validation-20260528.md) — Protocol doc #6 (mid). CLEANEST pre-v5 protocol doc.
- [TGMessage-Routing Validation 2026-05-28](tgmessage-routing-validation-20260528.md) — Protocol doc #7 (mid). THREE routing mechanisms confirmed (not two): C++ per-handler "Forward" relay (FUN_0069fda0 et al), Python "NoMe" relay (≥0x2C), connect-event broadcast via FUN_006b51e0 (transport-only). MATERIAL CORRECTIONS: FUN_006b63a0 is CONNECT handler not type-0x00 game-data relay (doc's "Host Relay Path" section needs re-architect); "NoMe" + "Forward" groups CREATED by MultiplayerGame_Ctor at 0x0069E590 (xrefs 0x0069E6FA + 0x0069E716), NOT by Python; 0x006b8530 is TGBufferStream_GetBufferAndSize NOT TGMessage::GetData. SendTGMessage 3 modes: -1 via FUN_006bb9d0 lookup by peer+0x1C key (NEW: peer has TWO IDs — +0x18 persistent network ID, +0x1C session/connection token), 0 broadcast, >0 binary-search peer+0x18. Jump table 41 entries fully decoded — 0x06+0x0D both PythonEvent → FUN_0069f880 LOCAL-ONLY; 0x0C+0x11+0x12 share `PUSH 0 → FUN_0069fda0` (preserve wire eventCode). Audit cross-source: per-opcode relay observations match binary handler decisions exactly. Status: partial. Open Q: chat echo 1:2 ratio for 0x2C unexplained by NoMe-only relay. ONE material correction: WriteCString writes uint32 LE length prefix (vtable+0x6c WriteLong), NOT uint16 LE — stock never calls it (uses explicit WriteShort+Write) so trace observations unaffected. Two naming clarifications (TGMessage::Serialize not WriteToBuffer; ProcessIncomingPackets not Messages). BYTE-LEVEL CONFIRMED: MAX_MESSAGE_TYPES=0x2B init at 0x00654f31 (raw bytes verified); ET_NETWORK_MESSAGE_EVENT 0x60001 at 3 sites; TGMessageEvent sizeof 0x2C (PUSH 0x2C at 0x006b4794); "NoMe"+"Forward" group-name strings at 0x008e5528/0x008d94a0; SetGuaranteed SETNZ-to-[+0x3A] pattern; SendTGMessage 3-mode targetID routing (-1/=0/>0). 8 renames + 5 prototypes + 6 plate comments. Pattern: SWIG wrappers often lack fn entries — find via `PUSH <fmt_addr>` byte search (`68 LL LL LL 00`). Cross-source tags needed on 3 routing claims (relay-audit-20260224 corroboration for 0x2C 1:2 echo / 0x36 always-paired / 0x37 per-join).
- [StateUpdate Validation 2026-05-28](stateupdate-validation-20260528.md) — Protocol doc #8 (mid). Highest-volume packet (30K+/session). Sender Ship__WriteStateUpdate 0x005B17F0 (slot 72, 2,472 bytes) + Receiver Ship__ReadStateUpdate 0x005B21C0 (slot 73, 1,539 bytes) + Dispatcher MpgameHandleStateUpdate 0x0069FF50 (jump-table slot 28). ZERO material wire-format corrections — all 8 dirty-bit payload sizes verified byte-by-byte. CRITICAL decompiler caveat: Ship_WriteStateUpdate flag-0x01 hash-bit emission tests `bVar16` (=!isMultiplayer), NOT visually-similar bVar2 — disasm at 005b1c76 shows BL is RELOADED. Confirms subsystem-integrity-hash.md "dead in MP" claim. PowerSubsystem__WriteState (0x005644B0) CREATED in Ghidra (was undefined-fn but doc had correct address); Format 3 = base + 2 battery bytes verified. C→S 0x80 / S→C 0x20 flag-direction split derives naturally from friendly-fire+player-count gate. SUBSYSTEM and WEAPON share SAME ship+0x284 linked list (filtered by IsWeaponType vtable[+8](0x801C)). Status: verified after 5 clarifications (none change wire format). Cross-anchors verify foundation cascade.
- [Object-Replication Validation 2026-05-28](object-replication-validation-20260528.md) — Protocol doc #9 (mid, smallest). 6/6 claims confirmed. FUN_0069F620 renamed MpgameHandleObjCreate; jump-table thunks 0x0069F31E (PUSH 0) + 0x0069F334 (PUSH 1) confirmed; wire format byte-by-byte off 1=owner_slot + off 2=team_id (iff opcode==0x03); active-slot SWAP pattern (DAT_0097fa84/8c) wraps FUN_005A1F50 (ReadInt class, ReadInt obj_id, factory FUN_006F13E0, vtable[0x118] Deserialize + vtable[0x11C] Fixup); host relay loops 16 PlayerSlots stride 0x18; Network controller via NiAlloc(0x58) + FUN_0047dab0 attached via vtable+0x134. Two REFINEMENTS (not corrections): (R1) FUN_006A19A0 is SENDER-side only — doc reads as if receiver calls it; receiver reads pre-mapped byte. (R2) doc's `vtable[0x10C]` is the SENDER serialize slot — receiver uses 0x118/0x11C inside FUN_005A1F50. Senders confirmed: NewPlayerInGameHandler (0x006A1E70) on join + FUN_006A02A0 (RequestObj 0x1E response). Authority S→C confirmed; "BIDIRECTIONAL" hypothesis in task prompt was misreading (C→S observations are host-relay echoes). Pattern: receiver-only decompile insufficient — must cross-check sender side for symmetric wire-format anchor.
- [ObjCreate-Serialization Validation 2026-05-28](objcreate-serialization-validation-20260528.md) — Protocol doc #10 (mid, heaviest at ~80 claims). THREE material corrections: (C1) velocity bytes 37-43 are CV4 (3 direction bytes + 4 magnitude float), NOT "f32 speed + 3 padding bytes" — doc has the total width right (7 bytes) but order/semantics wrong; (C2) MultiplayerGame.playerSlots base is +0x74 not +0x84 (MultiplayerGame_Ctor calls `FUN_00859d64(this+0x1d, 0x18, 0x10, ...)` = base+0x74, stride 24, capacity 16; the +0x84 the doc cites is `playerSlots[0]+0x10` game-state field); (C3) vtable[+0x118] reads ONLY 1-byte species + invokes Python InitObject, NOT position/quat/velocity/names — those are read by vtable[+0x11C] (FUN_005A2060). Two-pass scheme is FORCED by data-dependency (subsystems must exist before subsystem-state body can deserialize). Orientation IS quaternion (4 floats) — confirmed via FUN_00816390 (matrix→quat) and FUN_008162B0 (quat→matrix). Species map (SpeciesToShip 1..45 / SpeciesToTorp 1..15 / SpeciesToSystem 1..9) confirmed byte-exact against scripts. 11 functions renamed + 4 plate comments. Pattern: ctors (MultiplayerGame_Ctor) are AUTHORITY for struct layout via the allocator call shape. FPU register tracking in decompiler is brittle on writers — READ side decompiles cleanly because separate floats live in stack slots; use receive-side to anchor wire format when send-side is tangled.
- [StateUpdate-Subsystem-Wire-Format Validation 2026-05-28](stateupdate-subsystem-wire-format-validation-20260528.md) — Protocol doc #11 (mid). Wire formats (Base/Powered/Power WriteState) 100% confirmed byte-by-byte; round-robin algorithm matches doc pseudocode instruction-for-instruction; PowerSubsystem battery writes ARE unconditional (no TEST/JCC). TWO material corrections: (C1) named ship-slot table — ship+0x2C0 ShieldGenerator MISSING, ship+0x2C4 mislabelled as "PowerSubsystem (reactor)" but binary shows it's HullSubsystem (the reactor is at ship+0x2B0); (C2) EndMarker function misattribution — doc says no-op at 0x006CDAE0, but actual `vtable[+0xD8]` call resolves to 0x006CF9B0 GetPos (also discards return → no-op effective). PowerSubsystem_ReadState CREATED in Ghidra (was undefined, only DATA xref from vtable @ 0x0088A264). Engine parent-child via property+0x48 (EP_IMPULSE=0 → ship+0x2CC, EP_WARP=1 → ship+0x2D0) confirmed at 0x005B5097. 8 types excluded from second list (ship+0x29C) confirmed via 8-deep nested type-ID check. Sovereign-class example unverifiable (hardpoint file absent from repo). Pattern: vtable-slot confusion across classes (a no-op at slot X in class A doesn't mean it's the function called by code using class B at slot X); always verify the vtable address before naming the slot target. 14 renames + 7 plate comments.
- [Per-Ship Subsystem Wire-Format Validation 2026-05-28](per-ship-subsystem-validation-20260528.md) — Protocol doc #12 (mid, largest at ~250 claims). Sampling strategy: 4 ships verified byte-by-byte (Sovereign 49/Bird of Prey 32/Galor 31/Akira 47); cycle-byte math matches doc summary table EXACTLY; AddToSet→top-level order hypothesis holds for all 4. ZERO material wire-format corrections. ONE clarification: 1+N+2 PoweredSubsystem cycle byte formula treats bit+byte as ~2 bytes (actual wire is 1 bit hasData + 1 byte powerPct; round-robin uses stream-cursor 10-byte cap so approximation is per-tick accurate). Foundation mid #11 slot table holds: ship+0x2BC=PulseWeaponSystem (case 0x812F branch iVar4==3) + ship+0x2D4=TractorBeamSystem (case 0x812F branch iVar4==4). Per-ship doc never cites ship+offset directly so foundation corrections affect related docs only. Pattern: sampling N=4 with structural+ordering+special-case+foundation cross-anchor checks justifies high-confidence extrapolation across 16 ships.
- [TGObjPtrEvent Validation 2026-05-28](tgobjptrevent-validation-20260528.md) — Protocol doc #13 (LAST mid-tier). Class layout 0x2C + wire format 21B + all 11 game event types + 30 ctor xrefs + 5 vtable-DATA xrefs + dual-fire (Phaser/Tractor) + single-fire (Torpedo) + host-only gate on 0x008000DC ALL byte-by-byte CONFIRMED. ZERO wire-format / 0 producer corrections. THREE non-wire corrections: (C1) "TGSubsystemEvent (0x101)" FABRICATED — string search 0 hits; 0x101 IS TGEvent base itself (vtable 0x00895FF4, GetFactoryID emits 0x101 at 0x006d5ce0); flatten doc hierarchy. (C2) Vtable slot 0 = ScalarDeletingDtor at 0x00403320 (not 0x00403310); 0x00403310 is a THIRD RTTI string method "TGObjPtrEventPtr" (SWIG pointer-typeinfo, distinct from "_p_TGObjPtrEvent"); full vtable = 17 slots through +0x40, not 12-14. (C3) SWIG wrapper addresses (0x005C7F10..0x005C80E0) not defined in current Ghidra DB — annotation scripts never applied; SWIG name strings exist but bodies unanchored. Universal SWIG triple-string RTTI pattern observed (ClassName / _p_ClassName / ClassNamePtr) across TGCharEvent + ObjectExplodingEvent. 16 renames + 1 struct (TGObjPtrEvent 0x2C/12 fields) + 3 plates. Pattern: `FUN_00717b70(0x2c) → FUN_00718010 → FUN_00403290(0)` byte-sequence is the unique signature for TGObjPtrEvent producers; size literal 0x2C identifies it. Repair-priority producer FUN_005519e0 hand-rolls vtable assignment (manual ctor) instead of calling 0x00403290 — explains its presence in vtable-DATA xrefs but not in 30 ctor CALL xrefs.
- [PythonEvent Wire Format Validation 2026-05-28](pythonevent-wire-format-validation-20260528.md) — Protocol doc #14 (FIRST LEAF). ZERO wire-format corrections — all 4 class sizes (16/17/20/24 payload, 17/18/21/25 wire) byte-by-byte confirmed via WriteToStream decompiles + stream vtable slot map. Receiver MpgameHandlePythonEvent renamed at 0x0069F880 (handles both opcode 0x06 + 0x0D — single function, no opcode branch). 8-step receive flow confirmed. FIVE non-wire corrections: (C1) hierarchy fabrication cascade from doc #13 — "TGSubsystemEvent (0x101)" must be relabeled "TGEvent (0x101)" with hierarchy flattened to NiObject→TGEvent→{TGCharEvent, TGObjPtrEvent, ObjectExploding}; (C2) ObjectExploding IsA returns true for 3 IDs (0x8129, 0x101, 0x02) not 2 — doc missed 0x101 branch; (C3) source field WriteObjectRef encoding is 2-case (NULL/valid) not 3-case — only DEST field has sentinel branch in FUN_006D6130; (C4) "EventManager::PostEvent" naming is approximate — actual FUN_006DA300 is Event self-dispatch via event->dest_obj->vtable[+0x50] (NOT a global manager); (C5) in-memory size (0x30 ObjectExploding) vs wire size (24/25) ambiguity in doc text. Both producers HostEventHandler 0x006A1150 + ObjectExplodingHandler 0x006A1240 are UNDEFINED-in-DB but exist as raw code — disasm confirms byte-by-byte match to doc behavior ("NoMe" string at 0x008E5528, opcode 0x06 stack write, TGAlloc(0x40), msg+0x3A=1 reliable, SendTGMessageToGroup at 0x006B4DE0). Stream vtable SWIG variant @ 0x00895C58: confirmed +0x60/+0x64 = ReadInt/WriteInt (4 raw bytes each — NOT bit-packed SmallInt as initially feared); +0x80/+0x84 are virtual dispatchers forwarding to +0x68/+0x6C. Pattern: ObjectExploding wire extension uses raw +0x6C/+0x74 (Int/Float), NOT obj-ref encoded +0x84 like base. Open Q: collision message-count inconsistency (worked-example 14 vs 11+2-delayed sum of 13) — packet trace replay needed.
- [Set-Phaser-Level-Protocol Validation 2026-05-28](set-phaser-level-validation-20260528.md) — Leaf #16: TGCharEvent (0x105) wire 18B, FUN_006A1970 sender, FUN_00574180 applier; SWIG-registered handler pattern.
- [Delete-Player-UI Validation 2026-05-28](delete-player-ui-validation-20260528.md) — Leaf #17. Resolves OQ #2 (factory 0x866). TWO REGISTRIES discovery: NiRTTI vs TGFactory (DAT_0099a578); 0x866 lives in TGFactory not NiRTTI. Class 0x866 is TGEvent SUBCLASS (vtable 0x00895848 size 0x2C, sibling 0x865/0x867); base TGEvent vtable IS 0x00895FF4. EventManager singleton base = 0x0097F838 (+0x2C registry = 0x0097F864 — both correct, different offsets). MATERIAL CORRECTIONS: (C1) "DeletePlayerHandler FUN_006a0ca0 sends 0x17" WRONG — that fn sends opcode 0x18; disconnect-side 0x17 sender = FUN_006b75b0 (posts 0x866 event with code 0x60005, fires LAB_0x006a1590 which marshals wire); (C2) "factory 0x866 = base TGEvent" WRONG — it's a subclass with 1 extra byte at +0x28; (C3) tgt_obj_id is NETWORK singleton handle (via FUN_006d62b0(this=network)), NOT ship/player; (C4) src_obj_id ALWAYS 0 (never set), not "typically 0". Wire format 18B byte-by-byte confirmed including stock trace example. Two functions both named "NewPlayerInGameHandler": 0x006a1e70 = opcode 0x2A receiver (defined fn), 0x006a1590 = event-fired wire sender (LAB_, undefined in DB) — same SWIG name registered for both via FUN_0069efe0. Pattern: SWIG-registered handlers systematically undefined-in-DB (same as leaves #13/#14/#15/#16).
- [Collision-Effect-Protocol Validation 2026-05-28](collision-effect-protocol-validation-20260528.md) — Protocol leaf #15. ROCK-SOLID doc. ZERO wire-format corrections. TWO byte-level corrections: (C1) "Event Registration" line 293 handler addr `0x005afab0` should be `0x005AFAD0` (main table line 318 has it correct; 2-byte typo). (C2) Doc's "FUN_006da2a0 posts to DAT_0097f838 queue" is technically TGEventManager::PostEvent (__thiscall, this=0x97F838) — `MOV ECX,0x97F838` at 0x006A25ED IS the singleton-this load (clarification only). All damage constants byte-by-byte CONFIRMED: dead-zone 0.01f (0x00888A78=3C23D70A), HP scale 900.0f (0x008944BC=44610000), HP base 500.0f (0x008944B8=43FA0000), force-scale 1.5f (0x3FC00000 arg literal), distance gap 26.0f (0x008955C8=41D00000). Distance algorithm: FSQRT(deltas) - vtable+0xE4 GetModelBound[+0xC] for both ships - FCOMP; if gap >= 26.0f REJECT (JZ at 006A25EA). EventManager re-tag at event+0x10 to 0x008000FC. Vtable at 0x0089395C (17 slots, +0x00..+0x40) ALL match doc rows. Real-code-but-undefined functions follow leaf #14 SWIG pattern: 0x005871A0 WriteToStream (prologue `83 EC 30 53 55`), 0x00587300 ReadFromStream (prologue `83 EC 14 56 57`), 0x00586D00 ctor (prologue `6A FF 68 48 A7 87 00`), 0x005AF9C0 client send-side handler, 0x006D29A0/0x006D2D10 stream Compress* primitives — all have valid prologues, vtable-DATA-only xrefs (no plain CALLs explain auto-analysis miss). Server-authority semantics CONFIRMED: NO recomputation; geometry+identity validation only; client-supplied contact bytes + force float accepted as-is. Confirms CLAUDE.md "Collision damage authority inverted" — stock DOES do bounding-sphere gap check (the 26.0f constant), OpenBC misses that. Opcode 0x15 jump-table thunk = 0x0069F491 (index 0x13 = opcode 0x15 - 2 bias, offset 0x4C from 0x0069F534), NOT 0x0069F4A5 (= opcode 0x17 DeletePlayerUI). 6 renames + 7 labels + 1 prototype + 1 plate. Status: verified after 2 minor corrections.
